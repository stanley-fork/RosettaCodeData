import extensions;

public Program()
{
    auto s := emptyString;

    if (s.isEmpty())
        { Console.printLine("'", s, "' is empty") };

    if (s.isNonempty())
        { Console.printLine("'", s, "' is not empty") }
}
