function(acc)
    = (n => acc.append(n));

Accumulator(n)
    = function(new Variable(n));

public Program()
{
    var x := Accumulator(1);

    x(5);

    var y := Accumulator(3);

    Console.write(x(2.3r))
}
